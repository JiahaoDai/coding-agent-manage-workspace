## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues; use the `gh` CLI to read and write them. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### design docs
before you act, you must obey the `docs/design.md`

### UI design
reference chatgpt UI design.

### api_key
api_key is store in `.env`,only use for test claude code adapter